---
phase: 03-real-time-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.ts
autonomous: true
requirements:
  - JOIN-02
  - SYNC-02

must_haves:
  truths:
    - "When a participant sends { type: 'join', participantName } over WebSocket, their name appears in session.participants"
    - "All sockets in the session room receive a full session-snapshot broadcast after a participant joins"
    - "A participant whose name is already in the list (reconnect) is not added twice; idempotent join works"
    - "Invalid or missing participantName is silently ignored; no server crash"
  artifacts:
    - path: "server.ts"
      provides: "ws.on('message') handler that processes join messages and broadcasts updated snapshot"
      contains: "ws.on('message'"
  key_links:
    - from: "server.ts (ws.on message handler)"
      to: "sessionStore.broadcast()"
      via: "sessionStore.getData() + broadcast()"
      pattern: "sessionStore\\.broadcast\\("
    - from: "server.ts (ws.on message handler)"
      to: "session.participants"
      via: "push() with includes() guard"
      pattern: "participants\\.push\\("
---

<objective>
Add the WebSocket message handler to server.ts that receives `{ type: 'join', participantName }` from participants, appends their name to `session.participants` (idempotent — no duplicates on reconnect), and broadcasts the full state snapshot to every socket in the room.

Purpose: This is the only missing piece of server infrastructure for Phase 3. The session store, broadcast helper, full-state snapshot on connect, and socket lifecycle management all exist from Phase 1. Adding the `ws.on('message')` handler is the single change that enables JOIN-02 (all participants see who joined) and SYNC-02 (reconnect delivers full state).

Output: Modified `server.ts` with a `ws.on('message')` handler inside `wss.on('connection')`.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-time-layer/03-RESEARCH.md
@server.ts
@types/index.ts
@lib/session-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ws.on('message') join handler inside wss.on('connection') in server.ts</name>
  <files>server.ts</files>
  <action>
    Inside the existing `wss.on('connection', (ws, req) => { ... })` block in server.ts, add a `ws.on('message')` handler immediately after the existing `ws.on('close')` and `ws.on('error')` handlers (or after the initial snapshot send — placement within the connection block is fine as long as it is inside the block).

    The handler must:

    1. Parse raw bytes: `let msg: unknown; try { msg = JSON.parse(raw.toString()) } catch { return }`

    2. Guard for valid join message shape:
       ```typescript
       if (
         typeof msg !== 'object' || msg === null ||
         (msg as any).type !== 'join' ||
         typeof (msg as any).participantName !== 'string'
       ) return
       ```

    3. Extract and trim the name: `const name = ((msg as any).participantName as string).trim(); if (!name) return`

    4. Look up the session: `const session = sessionStore.get(sessionId!); if (!session) return`

    5. Append idempotently (no duplicate if reconnecting with same name):
       ```typescript
       if (!session.participants.includes(name)) {
         session.participants.push(name)
       }
       ```

    6. Broadcast full snapshot to all sockets in the room using `sessionStore.getData()` (not manual destructuring — keep consistent with DATA CONTRACT):
       ```typescript
       const data = sessionStore.getData(sessionId!)
       if (data) {
         sessionStore.broadcast(sessionId!, { type: 'session-snapshot', data })
       }
       ```

    The `sessionId` variable is already in scope from the outer `wss.on('connection')` closure (set at line 19 via `url.searchParams.get('session')`).

    Do NOT change any existing code. Do NOT add TypeScript `as ClientMessage` cast unless ClientMessage is already imported — import it if needed (`import type { SessionData, ClientMessage } from '@/types'`). Keep the handler focused: parse, validate, update, broadcast. No logging needed.

    Note: The existing snapshot in `wss.on('connection')` uses `const { sockets, ...data } = session` — do NOT copy this pattern for the join handler. Use `sessionStore.getData(sessionId!)` instead, which is the canonical serialization method that strips non-serializable fields. This is consistent with the getData() pattern from STATE.md decisions.
  </action>
  <verify>
    Run `npx tsc --noEmit 2>&1 | head -20` from the project root to confirm no TypeScript errors in server.ts.

    Then start the dev server: `npm run dev` (uses tsx watch server.ts). In a second terminal:
    1. Create a test session: `curl -s -X POST http://localhost:3000/api/sessions -H 'Content-Type: application/json' -d '{"items":[{"id":"i1","name":"Burger","priceCents":1000,"qty":1}],"taxCents":80,"tipCents":150}' | jq .`
    2. Copy the `sessionId` from the response.
    3. Open a WebSocket connection and send a join message using wscat or a quick Node.js script:
       ```bash
       node -e "
       const ws = new (require('ws'))('ws://localhost:3000/ws?session=SESSION_ID');
       ws.on('open', () => ws.send(JSON.stringify({ type: 'join', participantName: 'Alice' })));
       ws.on('message', (d) => { console.log(JSON.parse(d)); ws.close(); });
       "
       ```
    4. Confirm the second message (after initial snapshot) includes `participants: ['Alice']`.
    5. Send again with same name — confirm participants list still shows `['Alice']` (deduplication).
  </verify>
  <done>
    Server receives `{ type: 'join', participantName: 'Alice' }`, appends 'Alice' to participants, and broadcasts a session-snapshot where `data.participants` includes 'Alice'. Sending join again with 'Alice' does not create a duplicate. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors in server.ts
2. WebSocket join flow works end-to-end: connect, send join, receive snapshot with participant name in list
3. Idempotent join confirmed: same name sent twice results in single entry in participants array
4. Invalid join messages (empty name, wrong type, malformed JSON) are silently ignored without crashing server
</verification>

<success_criteria>
- `ws.on('message')` handler exists inside `wss.on('connection')` in server.ts
- Handler parses join messages, appends participant name (idempotent), broadcasts full snapshot via sessionStore.getData() + sessionStore.broadcast()
- TypeScript compiles without errors
- Manual WebSocket test confirms participants array updated and broadcast received
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-time-layer/03-01-SUMMARY.md`
</output>
