# Phase 5: Summary and Finalization — Discussion Log

**Session date:** 2026-04-08
**Mode:** discuss (interactive)

---

## Area: Host Identity

**Q: How should the system track which participant is the host?**
Options: Store hostName at session creation / Separate host route / hostName in URL param only
→ **Store hostName at session creation** (recommended default)

**Q: When the host joins /session/[id], how do they enter their name?**
Options: Pre-filled from URL param ?name=Alice / Host skips JoinForm entirely
→ **Pre-filled from URL param ?name=Alice** (recommended default)

---

## Area: Summary Navigation

**Q: How should participants transition to the summary screen after Finalize?**
Options: Same page new screen state / Navigate to /session/[id]/summary
→ **Same page, new screen state** — matches existing state machine pattern

**Q: After finalization, should participants be able to go back to the claiming screen?**
Options: No (permanent) / Yes (allow un-finalize)
→ **No — finalization is permanent**

---

## Area: Unclaimed Items Dialog

**Q: When does the unclaimed items choice appear?**
Options: Blocking modal before finalization / Only if items exist / Always show confirmation
→ **Blocking modal before finalization** (recommended)

**Q: Confirm: FINAL-02 requires two options — offer both or simplify?**
Options: Offer both choices in modal / Always split (deviate from FINAL-02)
→ **Offer both choices in the modal (matches FINAL-02)**
  - "Split among everyone" — distribute proportionally
  - "I'll cover the rest" — host absorbs unclaimed items

---

## Area: Participant Summary Content

**Q: What does each participant's summary screen show?**
Options: Full breakdown (subtotal + tax + tip + total) / Total only
→ **Full breakdown** — food subtotal, tax share, tip share, total owed

**Q: What does the host see on their summary screen?**
Options: Own breakdown + participant table / Table only / Same as participants
→ **Host's own breakdown + a table of all participants**

---

*Log generated by /gsd-discuss-phase on 2026-04-08*
