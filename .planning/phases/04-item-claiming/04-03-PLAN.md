---
phase: 04-item-claiming
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false
requirements:
  - CLAIM-01
  - CLAIM-02
  - CLAIM-03
  - CLAIM-04
  - SYNC-01

must_haves:
  truths:
    - "All five Phase 4 success criteria observable in the running app"
    - "Claim from one phone appears on another within two seconds"
    - "Shared item cost splits correctly and updates live"
  artifacts:
    - path: "components/session/ClaimableItem.tsx"
      provides: "Claimable item row component"
    - path: "components/session/SessionRoom.tsx"
      provides: "Upgraded session room with claiming, footer, reconnect, finalize"
  key_links:
    - from: "SessionRoom.tsx onClaim"
      to: "server.ts ws.on('message') claim branch"
      via: "WebSocket send + broadcast back to all sockets"
      pattern: "type.*claim"
---

<objective>
Human verification of the complete Phase 4 item-claiming flow across two browser tabs. Confirms all five success criteria from the ROADMAP are observable in the running app before Phase 5 planning begins.

Purpose: Phase gate — Phase 5 (Summary and Finalization) depends on claims working correctly end-to-end.
Output: Verified confirmation that claiming, syncing, shared splits, and claimant names all work correctly.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-item-claiming/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 4 item-claiming feature:
- Claimable item rows with four visual states (unclaimed / mine / shared / theirs)
- Tap to claim; tap again to unclaim (CLAIM-01)
- Shared item cost split shown as "$X.XX ÷ N = $Y.YY" (CLAIM-02)
- Two rows from qty-expanded items claimable independently (CLAIM-03)
- Claimant names shown on each row, visible to all participants (CLAIM-04)
- Real-time sync: claims from one device appear on all others within 2 seconds (SYNC-01)
- Pinned footer with running total per participant
- Host-only Finalize button
- Reconnect banner on unexpected disconnect
  </what-built>
  <how-to-verify>
Run `npm run dev` if not already running.

**Setup (do once):**
1. Navigate to http://localhost:3000/host
2. Capture a receipt (or use USE_OCR_MOCK=true to get 4 mock items)
3. Create the session — note the share URL
4. Open the share URL in TWO separate browser tabs (or two devices on the same network)
5. In Tab A, enter name "Alice" and join
6. In Tab B, enter name "Bob" and join

**Test CLAIM-01 (tap to claim / unclaim):**
7. In Tab A, tap any item row — it should highlight green ("mine")
8. Tap the same item again in Tab A — it should return to unclaimed (white/plain)

**Test CLAIM-02 (shared item cost split):**
9. In Tab A, tap the first item — Alice claims it
10. In Tab B, tap the SAME item — Bob claims it too
11. Both tabs should show "$X.XX ÷ 2 = $Y.YY" split price for that item
12. Alice's footer and Bob's footer should each show half the item price

**Test CLAIM-03 (qty-expanded items claimable independently):**
13. If OCR mock is on, two "Burger" rows exist (or use a qty:2 item from real receipt)
14. In Tab A, claim the first Burger row
15. In Tab B, claim the second Burger row
16. Each person shows one full Burger price in their footer — not a shared split

**Test CLAIM-04 (claimant names visible to all):**
17. With Alice claiming item 1 and Bob claiming item 2, check BOTH tabs
18. Both tabs must show "Alice" on item 1's row and "Bob" on item 2's row

**Test SYNC-01 (real-time, under 2 seconds):**
19. In Tab A, claim an unclaimed item — check Tab B within 2 seconds
20. Tab B must show the update without any reload

**Test footer and Finalize button:**
21. Confirm both tabs show "Your total: $X.XX" pinned at the bottom
22. If the host tab is open (separate from participant tabs), confirm it shows a "Finalize" button
23. Regular participant tabs must NOT show the Finalize button

**Expected outcome:** All 7 checks pass with no crashes, no React errors in the browser console.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all five ROADMAP Phase 4 success criteria are observable in the running app.
</verification>

<success_criteria>
User types "approved" after verifying all claiming behaviors work correctly across two browser tabs in real time.
</success_criteria>

<output>
After completion, create `.planning/phases/04-item-claiming/04-03-SUMMARY.md`
</output>
