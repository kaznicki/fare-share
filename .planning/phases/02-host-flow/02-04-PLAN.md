---
phase: 02-host-flow
plan: "04"
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements: [OCR-01, OCR-02, OCR-03, OCR-04, CORR-01, CORR-02, CORR-03, CORR-04, CORR-05, SESS-01, SESS-02, SESS-03]

must_haves:
  truths:
    - "Host completes the full flow end-to-end: camera → OCR → review → share"
    - "All 12 Phase 2 requirements are user-observable"
  artifacts:
    - path: "app/host/page.tsx"
      provides: "Three-screen state machine"
      exports: ["default HostPage"]
    - path: "components/host/CameraCapture.tsx"
      provides: "Camera + OCR submit"
      exports: ["default CameraCapture"]
    - path: "components/host/OcrReview.tsx"
      provides: "Item list editor + session creation"
      exports: ["default OcrReview"]
    - path: "components/host/ItemRow.tsx"
      provides: "Inline item editing row"
      exports: ["default ItemRow"]
    - path: "components/host/TaxTipFields.tsx"
      provides: "Tax/tip dollar inputs"
      exports: ["default TaxTipFields"]
    - path: "components/host/ShareScreen.tsx"
      provides: "QR code + clipboard share"
      exports: ["default ShareScreen"]
  key_links:
    - from: "app/host/page.tsx"
      to: "components/host/CameraCapture.tsx"
      via: "onComplete callback advancing screen state"
      pattern: "setScreen.*reviewing"
    - from: "components/host/OcrReview.tsx"
      to: "/api/sessions"
      via: "fetch POST with items/taxCents/tipCents"
      pattern: "fetch.*api/sessions"
    - from: "components/host/ShareScreen.tsx"
      to: "qrcode.react"
      via: "QRCodeSVG component"
      pattern: "QRCodeSVG"
---

<objective>
Human verification of the complete Phase 2 host flow. All automated implementation is done by Plans 01-03. This plan verifies all 12 requirements are met before marking Phase 2 complete.

Purpose: Gate on human confirmation that the full host experience works correctly end-to-end on a real mobile device or browser.
Output: Confidence that Phase 2 is shippable and Phase 3 can proceed.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete Phase 2 host flow end-to-end</name>
  <action>Human verification — follow how-to-verify steps to confirm all 12 Phase 2 requirements are working correctly in the browser.</action>
  <what-built>
    Complete Phase 2 host flow:
    - Camera capture UI with image preview and retake (OCR-01, OCR-02)
    - OCR extraction via POST /api/ocr with mock mode (OCR-03)
    - OCR failure handling with error banner and empty-list fallback (OCR-04)
    - Inline item editing: name, price (tap-to-edit), qty (stepper), delete, add (CORR-01, CORR-02, CORR-03)
    - Tax and tip editable fields pinned at the bottom (CORR-04)
    - Qty > 1 items expanded into separate rows on session creation (CORR-05)
    - Session creation via POST /api/sessions (SESS-01)
    - QR code display using qrcode.react (SESS-02)
    - Copy link button with clipboard API (SESS-03)
  </what-built>
  <how-to-verify>
    Start the dev server if not running: `npm run dev`

    **Test 1: Basic flow with mock OCR**
    1. Open http://localhost:3000/host in a browser
    2. Click "Take Photo" — file picker or camera opens
    3. Select any image file — preview appears
    4. Click "Retake" — preview disappears, same image can be re-selected (tests inputRef.current.value = '' reset)
    5. Select image again — preview appears again
    6. Click "Submit Receipt" — loading state shows ("Analyzing...")
    7. Review screen appears with 4 mock items (Margherita Pizza $12.99, Garlic Bread $4.99, etc.)

    **Test 2: OCR correction (CORR-01 through CORR-04)**
    8. Tap the name of item 1 — input appears. Change name. Tap elsewhere — new name saved.
    9. Tap the price of item 2 — number input appears. Change price. Tap elsewhere — price updates (shown as dollars).
    10. Tap + button on item 3 qty — qty increases to 2. Tap - — qty returns to 1. Tap - again — qty stays at 1 (minimum).
    11. Click the delete button on item 4 — row disappears from list.
    12. Click "Add Item" — blank row appears with cursor in the name field.
    13. Edit the Tax field in the bottom footer — value updates.
    14. Edit the Tip field — value updates.

    **Test 3: Session creation and qty expansion (SESS-01, CORR-05)**
    15. Set qty of one item to 2.
    16. Click "Create Session" — button shows "Creating..." briefly, then share screen appears.
    17. curl verify: `curl -s http://localhost:3000/api/sessions/{sessionId from network tab}` — the item with qty 2 should appear as 2 separate items with qty 1 each.

    **Test 4: Share screen (SESS-02, SESS-03)**
    18. Share screen shows a QR code (visible, scannable-looking square pattern).
    19. Share screen shows the join URL as text (e.g. `http://localhost:3000/session/...`).
    20. Click "Copy link" — button shows "Copied!" for ~2 seconds, then resets.
    21. Paste into another tab — URL format is correct.

    **Test 5: OCR failure path (OCR-04)**
    22. Navigate back to /host (refresh).
    23. Set USE_OCR_MOCK=false in .env.local temporarily and restart server.
    24. Select image and submit — error banner appears ("Could not read the receipt...") with "Continue anyway" option.
    25. Click "Continue anyway" — review screen appears with empty list and "OCR extracted 0 items" banner.
    26. Restore USE_OCR_MOCK=true and restart.

    **Expected outcomes:**
    - All 5 tests pass without console errors
    - UI is usable on mobile viewport (resize browser to 375px width to simulate phone)
    - Prices always display in dollar format with 2 decimal places
    - QR code is visually distinct (black/white squares pattern)
  </how-to-verify>
  <verify>All 5 test sequences complete without console errors. Human types "approved".</verify>
  <done>Human has confirmed all 12 Phase 2 requirements work correctly in the browser.</done>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed and what behavior you observed.</resume-signal>
</task>

</tasks>

<verification>
All Phase 2 requirements verified by human:
- OCR-01: Camera opens from within app
- OCR-02: Preview and retake flow works
- OCR-03: Items appear from OCR (mock) output
- OCR-04: Error banner with continue-anyway path
- CORR-01: Inline name edit
- CORR-02: Delete row
- CORR-03: Add blank item
- CORR-04: Tax and tip edit
- CORR-05: qty > 1 items expanded server-side
- SESS-01: Session created, sessionId returned
- SESS-02: QR code visible on share screen
- SESS-03: Copy link copies correct URL
</verification>

<success_criteria>
Human has confirmed "approved" after completing all 5 verification tests. Phase 2 is marked complete in ROADMAP.md and STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/02-host-flow/02-04-SUMMARY.md`
</output>
