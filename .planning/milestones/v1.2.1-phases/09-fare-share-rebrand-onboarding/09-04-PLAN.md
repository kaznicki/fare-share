---
phase: 09-fare-share-rebrand-onboarding
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - components/session/JoinForm.tsx
  - package.json
  - server.ts
  - types/index.ts
  - lib/bill-split.ts
  - README.md
autonomous: true
requirements:
  - BRAND-01
  - ONBOARD-03
  - ONBOARD-04

must_haves:
  truths:
    - "Guest join page shows the locked one-line app description 'Fare Share splits a restaurant bill by the items each person ordered.' between the hero lockup (added in 09-03) and the join card"
    - "Guest join page shows the locked four-step instruction list above the join card, with the four steps in the locked order and verbatim copy"
    - "Zero remaining user-visible 'Tab Splitter' strings in tracked source — git grep -i 'tab splitter' over components/, app/, server.ts, types/, lib/, package.json, README.md returns no matches"
    - "package.json name is 'fare-share' (kebab-case)"
    - "server.ts startup log says 'Fare Share ready on http://localhost:...'"
    - "types/index.ts and lib/bill-split.ts JSDoc headers say 'Fare Share' (not 'Tab Splitter')"
    - "README.md contains no 'Tab Splitter' string (already canonical 'Fare Share' per scan; this plan re-verifies)"
  artifacts:
    - path: "components/session/JoinForm.tsx"
      provides: "App description <p> + four-step <ol> inserted between hero lockup (09-03) and join card"
      contains: "Fare Share splits a restaurant bill by the items each person ordered."
    - path: "package.json"
      provides: "Renamed package: 'fare-share'"
      contains: "\"name\": \"fare-share\""
    - path: "server.ts"
      provides: "Startup log uses Fare Share wording"
      contains: "Fare Share ready"
    - path: "types/index.ts"
      provides: "JSDoc header rebranded"
      contains: "Canonical domain types for Fare Share"
    - path: "lib/bill-split.ts"
      provides: "JSDoc header rebranded"
      contains: "Bill-splitting math engine for Fare Share"
    - path: "README.md"
      provides: "Verified Fare Share branding (no Tab Splitter string remains)"
  key_links:
    - from: "components/session/JoinForm.tsx description block"
      to: "ONBOARD-03 locked copy"
      via: "Verbatim string per UI-SPEC Copywriting Contract"
      pattern: "Fare Share splits a restaurant bill"
    - from: "components/session/JoinForm.tsx instructions <ol>"
      to: "ONBOARD-04 locked four-step list"
      via: "Four <li> children with verbatim ordered copy"
      pattern: "Tap any item you ordered"
    - from: "package.json name field"
      to: "BRAND-01 rename sweep"
      via: "kebab-case rename tab-splitter -> fare-share"
      pattern: "fare-share"
---

<objective>
Insert the locked guest-page onboarding copy (one-line app description + four-step instruction list) into `components/session/JoinForm.tsx` at the insertion point Plan 09-03 reserved between the hero lockup and the join card; complete the user-visible "Tab Splitter" -> "Fare Share" string rename across the source tree (package.json, server.ts, types/index.ts, lib/bill-split.ts, README.md verification).

Purpose: Plan 09-01 fixed `app/layout.tsx` metadata; Plan 09-03 mounted the persistent HeaderBar wordmark and added the JoinForm hero. The remaining user-visible "Tab Splitter" strings live in source files outside the rendered DOM (package metadata, server startup log, JSDoc tooltips that surface in IDEs and could ship through error messages, README). This plan eliminates them. The onboarding copy on the guest page is the last remaining ONBOARD-* requirement (ONBOARD-03, ONBOARD-04) — it must be inserted in the slot 09-03 documented so the two parallel-wave plans do not collide.

This plan runs in Wave 2 in parallel with Plan 09-03. There is exactly ONE file overlap: `components/session/JoinForm.tsx`. The collision is resolved by 09-03 placing only the hero block + repaint and 09-04 inserting only the description + instructions, with a documented insertion-point comment between them. Both plans treat the rest of the file as read-only. The execute-phase orchestrator must serialize the two writes to JoinForm.tsx (apply 09-03 first, then 09-04 picks up the file with the hero+comment+card structure already present and inserts the description + ol between the hero and the card).

Output: Edited `JoinForm.tsx` with description + ordered list inserted; six-file string-rename sweep for "Tab Splitter" -> "Fare Share".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-fare-share-rebrand-onboarding/09-CONTEXT.md
@.planning/phases/09-fare-share-rebrand-onboarding/09-PATTERNS.md
@.planning/phases/09-fare-share-rebrand-onboarding/09-UI-SPEC.md
@components/session/JoinForm.tsx
@package.json
@server.ts
@types/index.ts
@lib/bill-split.ts
@README.md
</context>

<interfaces>
<!-- Locked copy strings — VERBATIM. Any deviation fails the verification grep. -->

App description (ONBOARD-03 — UI-SPEC §Copywriting Contract -> Guest join page):
```
Fare Share splits a restaurant bill by the items each person ordered.
```

Four-step instruction list (ONBOARD-04 — UI-SPEC §Copywriting Contract -> Guest join page):
```
1. Enter your name to join.
2. Tap any item you ordered.
3. Tap shared items to split them with others.
4. When the host finalizes, you'll see exactly what you owe.
```

The apostrophe in step 4 is a STRAIGHT apostrophe in source ("you'll"), but JSX requires `&apos;` (or curly-apostrophe) inside text content to satisfy `react/no-unescaped-entities`. Use `you&apos;ll` in the JSX.

String-rename targets (locked from PATTERNS.md "String-Rename Targets" table):

| File | Current | Target |
|---|---|---|
| `package.json` line 2 | `"name": "tab-splitter",` | `"name": "fare-share",` |
| `server.ts` line 225 | `console.log(\`> Tab Splitter ready on http://localhost:${port}\`)` | `console.log(\`> Fare Share ready on http://localhost:${port}\`)` |
| `types/index.ts` line 1 | `// Canonical domain types for Tab Splitter.` | `// Canonical domain types for Fare Share.` |
| `lib/bill-split.ts` line 1 | `// Bill-splitting math engine for Tab Splitter.` | `// Bill-splitting math engine for Fare Share.` |
| `README.md` | (already says "Fare Share" per PATTERNS scan) | verify — no change expected |

Out-of-scope renames (DO NOT touch — internal symbols only, retained per CONTEXT decision F):
- `.planning/**` (history)
- `.claude/**` (worktrees, settings)
- `*.md` files inside `.planning/` (decisions, summaries)
- Type names like `TabSplitter*` (none found in scan)
- Function names, variable names
- Git history / commits

Insertion point in `JoinForm.tsx` (set by Plan 09-03):
```tsx
return (
  <div className="max-w-sm mx-auto w-full">
    {/* Hero lockup — ONBOARD-02 */}
    <div className="flex justify-center pt-6 pb-4">
      <img src="/logo-lockup.svg" alt="Fare Share" className="h-16 w-auto" />
    </div>

    {/* Plan 09-04 inserts the app description <p> and the 4-step <ol> here, between the hero and the card. Do not place anything else in this slot. */}

    <div className="bg-paper-deep rounded-2xl shadow-md p-6 flex flex-col gap-4">
      ...
```

Tailwind utilities used in this plan (all defined by Plan 09-02 `app/globals.css` `@theme inline`):
- `text-ink-2` — secondary text (description + instructions)
- `text-base`, `text-sm` — typography sizing per UI-SPEC §Typography
- `mb-4`, `mb-6`, `space-y-2` — spacing per UI-SPEC §Spacing Scale (note: UI-SPEC §Spacing Scale "Phase-specific exceptions" -> instructions list resolved decision: use `space-y-2` (8px), NOT `space-y-1.5`)
- `list-decimal list-inside` — semantic ordered list with native ordinal numerals
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code -> bundled JSX | All onboarding copy and renamed strings are hard-coded literals at build time. No user input crosses into rendered text or package metadata. |
| package.json `name` field -> npm registry | Field is `"private": true` so the rename is metadata-only; package is never published. No supply-chain exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | `JoinForm.tsx` description + instruction copy | accept | Copy is hard-coded JSX text content. No template interpolation, no user-provided values. JSX safe-by-default — no `dangerouslySetInnerHTML`. |
| T-09-10 | Information disclosure | Renamed JSDoc strings in `types/index.ts`, `lib/bill-split.ts` | accept | Comments only; no executable code change, no behavior change, no data exposure. |
| T-09-11 | Denial of service | `package.json` name field rename | accept | Package is `"private": true`; rename does not affect Railway/Fly deploy because both providers key off the repo and `start` script, not the package name. Lockfile (`package-lock.json` or equivalent) does not exist as a separate concern in this repo (Next.js project; no separate lockfile management beyond what npm i regenerates). |

No HIGH severity threats. ASVS L1 satisfied — no new auth, no new input handlers, no new endpoints, no new database writes. Copy + metadata rename only.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert app description + four-step instruction list into JoinForm.tsx between hero and card</name>
  <files>components/session/JoinForm.tsx</files>
  <read_first>
    - components/session/JoinForm.tsx (post-09-03 state — must already contain the `<div className="max-w-sm mx-auto w-full">` wrapper, the hero `<img src="/logo-lockup.svg" h-16>` block, and the comment placeholder "Plan 09-04 inserts the app description <p> and the 4-step <ol> here". If the comment is missing, Plan 09-03 has not yet run — orchestrator must serialize 09-03 before 09-04 on this file.)
    - .planning/phases/09-fare-share-rebrand-onboarding/09-UI-SPEC.md "Copywriting Contract -> Guest join page" section (locks every string in this task)
    - .planning/phases/09-fare-share-rebrand-onboarding/09-CONTEXT.md "Decisions -> C. App description + guest instructions copy" section (decision source)
    - .planning/phases/09-fare-share-rebrand-onboarding/09-UI-SPEC.md "Spacing Scale -> Phase-specific exceptions" (locks `space-y-2` for the ol — NOT `space-y-1.5`)
    - .planning/phases/09-fare-share-rebrand-onboarding/09-UI-SPEC.md "Accessibility Contract" (instructions list rendered as semantic <ol> with list-decimal list-inside)
  </read_first>
  <action>
    Replace the placeholder comment block in `JoinForm.tsx` (the comment line that reads `{/* Plan 09-04 inserts the app description <p> and the 4-step <ol> here, between the hero and the card. Do not place anything else in this slot. */}`) with the following two JSX blocks, in this order:

    ```tsx
    {/* App description — ONBOARD-03 (verbatim per UI-SPEC) */}
    <p className="text-center text-ink-2 text-base mb-4">
      Fare Share splits a restaurant bill by the items each person ordered.
    </p>

    {/* Usage instructions — ONBOARD-04 (verbatim per UI-SPEC) */}
    <ol className="text-sm text-ink-2 mb-6 space-y-2 list-decimal list-inside">
      <li>Enter your name to join.</li>
      <li>Tap any item you ordered.</li>
      <li>Tap shared items to split them with others.</li>
      <li>When the host finalizes, you&apos;ll see exactly what you owe.</li>
    </ol>
    ```

    **Critical rules:**

    - The four `<li>` strings are LOCKED — do not paraphrase, do not change punctuation, do not change capitalization. The trailing periods on each list item are intentional and must be preserved.
    - Use `&apos;` for the apostrophe in step 4 (`you&apos;ll`) — JSX requires the entity reference under the project's lint rules.
    - Description `<p>` text is LOCKED — full string is `Fare Share splits a restaurant bill by the items each person ordered.` (single sentence, trailing period included).
    - Description sits BEFORE the instructions list (description first, then ol).
    - Both blocks sit between the hero `<div>` and the card `<div className="bg-paper-deep ...">`. Do not move them.
    - Use `space-y-2` (NOT `space-y-1.5`) on the `<ol>` per UI-SPEC §Spacing Scale resolved decision.
    - `list-decimal list-inside` ensures screen readers announce "list of 4 items" and natively render the ordinal numerals — no custom counter / no manual numbering inside the `<li>` text.
    - Do NOT modify the hero block, the card, the form, the input, the Join button, the heading "Join the table", or any other part of the file.

    **DO NOT touch any other element of `JoinForm.tsx`.** The component logic (`useState`, `handleSubmit`, props) stays unchanged. The `'use client'` directive, imports, props interface, and the rest of the JSX tree all remain identical to the post-09-03 state.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('components/session/JoinForm.tsx','utf8'); const checks=[['hero still present',/<img src=\"\\/logo-lockup\\.svg\" alt=\"Fare Share\" className=\"h-16 w-auto\"/, s],['description verbatim',/<p className=\"text-center text-ink-2 text-base mb-4\">\\s*Fare Share splits a restaurant bill by the items each person ordered\\.\\s*<\\/p>/, s],['ol with locked classes',/<ol className=\"text-sm text-ink-2 mb-6 space-y-2 list-decimal list-inside\">/, s],['step 1',/<li>Enter your name to join\\.<\\/li>/, s],['step 2',/<li>Tap any item you ordered\\.<\\/li>/, s],['step 3',/<li>Tap shared items to split them with others\\.<\\/li>/, s],['step 4 with apos',/<li>When the host finalizes, you&apos;ll see exactly what you owe\\.<\\/li>/, s],['placeholder comment removed',/Plan 09-04 inserts/, s, true],['card still present',/bg-paper-deep rounded-2xl shadow-md p-6/, s],['Join the table heading still present',/Join the table/, s],['no space-y-1\\.5 (use space-y-2 per UI-SPEC)',/space-y-1\\.5/, s, true],['no curly apostrophe in step 4 source',/you\\u2019ll/, s, true],['ol order: description before ol',/<p[^>]*>\\s*Fare Share splits[\\s\\S]*<ol /, s]]; let fail=0; for(const [name,re,src,absent] of checks){const got=re.test(src); const want=absent?false:true; if(got!==want){console.error('FAIL:',name,'expected',want?'present':'absent'); fail++}} if(fail){process.exit(1)} console.log('OK: JoinForm onboarding copy inserted verbatim')"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep "Fare Share splits a restaurant bill by the items each person ordered." components/session/JoinForm.tsx` returns 1 match
    - `git grep "Enter your name to join." components/session/JoinForm.tsx` returns 1 match
    - `git grep "Tap any item you ordered." components/session/JoinForm.tsx` returns 1 match
    - `git grep "Tap shared items to split them with others." components/session/JoinForm.tsx` returns 1 match
    - `git grep "you&apos;ll see exactly what you owe." components/session/JoinForm.tsx` returns 1 match
    - `git grep "list-decimal list-inside" components/session/JoinForm.tsx` returns 1 match
    - `git grep "space-y-2" components/session/JoinForm.tsx` returns 1 match
    - `git grep "space-y-1.5" components/session/JoinForm.tsx` returns no matches
    - `git grep "Plan 09-04 inserts" components/session/JoinForm.tsx` returns no matches (placeholder consumed)
    - `git grep "bg-paper-deep" components/session/JoinForm.tsx` returns >= 1 match (card untouched)
    - `git grep "Join the table" components/session/JoinForm.tsx` returns 1 match (card heading untouched)
    - `npm run build` succeeds (no `react/no-unescaped-entities` error from the apostrophe)
    - `npx tsc --noEmit` succeeds
  </acceptance_criteria>
  <done>
    JoinForm renders, in DOM order: hero lockup -> description paragraph -> instruction list -> join card. All copy verbatim from UI-SPEC. Spacing utilities match the resolved UI-SPEC §Spacing Scale decision. Apostrophe entity-encoded for JSX. Card and form unchanged from Plan 09-03's output.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: String-rename sweep — package.json, server.ts, types/index.ts, lib/bill-split.ts, README.md verification</name>
  <files>package.json, server.ts, types/index.ts, lib/bill-split.ts, README.md</files>
  <read_first>
    - package.json (line 2 "name": "tab-splitter")
    - server.ts (around line 225 — the `httpServer.listen` callback's first console.log)
    - types/index.ts (line 1 — JSDoc comment)
    - lib/bill-split.ts (line 1 — JSDoc comment)
    - README.md (full file — confirm "Fare Share" already present, no "Tab Splitter" remains)
    - .planning/phases/09-fare-share-rebrand-onboarding/09-PATTERNS.md "String-Rename Targets" table (lines 487-510 — locked old/new pairs)
    - .planning/phases/09-fare-share-rebrand-onboarding/09-CONTEXT.md "Decisions -> F. Strings and metadata to rename" (out-of-scope notes)
  </read_first>
  <action>
    Apply five rename operations:

    **1. `package.json` line 2 — name field:**
    BEFORE: `"name": "tab-splitter",`
    AFTER: `"name": "fare-share",`

    Use kebab-case (`fare-share`, with hyphen). Do not change any other field. Do NOT add a `description` field unless one already exists; if a `description` field exists and contains "Tab Splitter", change it to "Fare Share" — but the current scan shows no `description` field, so no action needed there.

    **2. `server.ts` line 225 — startup log:**
    BEFORE (verbatim): `console.log(`> Tab Splitter ready on http://localhost:${port}`)`
    AFTER (verbatim): `console.log(`> Fare Share ready on http://localhost:${port}`)`

    The surrounding two console.logs ("WebSocket server on...", "Environment: ...") stay unchanged. Only the first line of the `httpServer.listen` callback changes.

    **3. `types/index.ts` line 1 — JSDoc header:**
    BEFORE: `// Canonical domain types for Tab Splitter.`
    AFTER: `// Canonical domain types for Fare Share.`

    Lines 2+ ("All monetary values are integer cents...", import statements, type declarations) all stay unchanged.

    **4. `lib/bill-split.ts` line 1 — JSDoc header:**
    BEFORE: `// Bill-splitting math engine for Tab Splitter.`
    AFTER: `// Bill-splitting math engine for Fare Share.`

    Lines 2-4 (the rest of the JSDoc block about LRM) stay unchanged.

    **5. `README.md` — verification only:**
    Per PATTERNS.md: README already says "Fare Share" — verified during pattern mapping. Re-run the verification grep:
    ```
    git grep -i "tab splitter" -- README.md
    ```
    Expected: empty. If the grep returns matches, replace each occurrence of "Tab Splitter" with "Fare Share" preserving casing (e.g., "Tab Splitter" -> "Fare Share", "tab splitter" -> "fare share", "TAB SPLITTER" -> "FARE SHARE", and the kebab-case "tab-splitter" -> "fare-share"). The expected outcome is that no edits are needed.

    **6. Final-sweep verification (no edit; re-run grep):**

    Run:
    ```
    git grep -i "tab splitter" -- ':!.planning' ':!.claude' ':!*.md.bak' ':!node_modules' ':!.next' ':!.git' ':!SUMMARY.md' ':!PROJECT_BRIEF.md' ':!course-structure.json' ':!lesson-modules' ':!receipt.jpg' ':!test-rece*'
    ```

    Expected: empty.

    Also run a hyphenated check for any internal name references (excluding history/planning):
    ```
    git grep "tab-splitter" -- ':!.planning' ':!.claude' ':!node_modules' ':!.next' ':!.git' ':!*.lock' ':!package-lock.json' ':!SUMMARY.md'
    ```

    Expected: empty (post-rename of `package.json`).

    If the final-sweep grep returns ANY match outside the excluded paths, investigate and rename per the same casing-preservation rule. Note in the SUMMARY any unexpected matches found and how they were resolved. The PATTERNS scan covered the full source tree and found only the five targets listed above, so the expected outcome is zero unexpected matches.

    **Out-of-scope (DO NOT touch):**
    - `.planning/**` files (decisions, summaries, prior-phase context — historical record)
    - `.claude/**` files (worktrees, settings)
    - Any `*.md` file under `.planning/`
    - `SUMMARY.md` at repo root if it exists (untracked working file per git status)
    - `PROJECT_BRIEF.md` at repo root if it exists (untracked working file)
    - `course-structure.json`, `lesson-modules/`, `receipt.jpg`, `test-rece*` (untracked working files)
    - Any type name, function name, variable name, JSX prop name (internal symbols retained per CONTEXT decision F)
    - Git history / commit messages
    - `package-lock.json` if it exists (lockfile auto-regenerates if name changes; do not hand-edit)

    After all five edits, optionally run `npm install` (if a lockfile exists locally) to regenerate the lockfile with the new name. If no lockfile exists at repo root, skip — the package name is metadata-only since `"private": true` is set.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const cp=require('child_process'); function read(f){return fs.readFileSync(f,'utf8')} const checks=[['package.json name fare-share',/\"name\":\\s*\"fare-share\"/, read('package.json')],['package.json no tab-splitter',/tab-splitter/, read('package.json'), true],['server.ts Fare Share ready',/console\\.log\\(`> Fare Share ready on http:\\/\\/localhost:\\$\\{port\\}`\\)/, read('server.ts')],['server.ts no Tab Splitter ready',/Tab Splitter ready/, read('server.ts'), true],['types/index.ts header',/^\\/\\/ Canonical domain types for Fare Share\\./, read('types/index.ts')],['types/index.ts no Tab Splitter',/Tab Splitter/, read('types/index.ts'), true],['lib/bill-split.ts header',/^\\/\\/ Bill-splitting math engine for Fare Share\\./, read('lib/bill-split.ts')],['lib/bill-split.ts no Tab Splitter',/Tab Splitter/, read('lib/bill-split.ts'), true],['README.md no Tab Splitter',/Tab Splitter/i, read('README.md'), true]]; let fail=0; for(const [name,re,src,absent] of checks){const got=re.test(src); const want=absent?false:true; if(got!==want){console.error('FAIL:',name,'expected',want?'present':'absent'); fail++}} if(fail){process.exit(1)} console.log('OK: 5 string-rename targets complete')"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep "\"name\": \"fare-share\"" package.json` returns 1 match
    - `git grep "tab-splitter" package.json` returns no matches
    - `git grep "Fare Share ready" server.ts` returns 1 match
    - `git grep "Tab Splitter" server.ts` returns no matches
    - `git grep "Canonical domain types for Fare Share" types/index.ts` returns 1 match
    - `git grep "Tab Splitter" types/index.ts` returns no matches
    - `git grep "Bill-splitting math engine for Fare Share" lib/bill-split.ts` returns 1 match
    - `git grep "Tab Splitter" lib/bill-split.ts` returns no matches
    - `git grep -i "tab splitter" README.md` returns no matches
    - `git grep -i "tab splitter" -- ':!.planning' ':!.claude' ':!node_modules' ':!.next' ':!.git' ':!SUMMARY.md' ':!PROJECT_BRIEF.md' ':!course-structure.json' ':!lesson-modules' ':!receipt.jpg' ':!test-rece*'` returns no matches (final phase-9 BRAND-01 acceptance grep)
    - `git grep "tab-splitter" -- ':!.planning' ':!.claude' ':!node_modules' ':!.next' ':!.git' ':!package-lock.json' ':!*.lock' ':!SUMMARY.md'` returns no matches
    - `npm run build` succeeds
    - `npm run dev` (sanity check, optional — startup log says "Fare Share ready on http://localhost:...")
  </acceptance_criteria>
  <done>
    All five user-visible "Tab Splitter" string sites renamed to "Fare Share" (or the kebab-case `fare-share` for `package.json`). Final-sweep grep across tracked source confirms zero remaining occurrences. Internal symbols, function names, type names, git history are unchanged. Build passes; `private: true` package metadata rename has no deploy impact.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `git grep -i "tab splitter" -- ':!.planning' ':!.claude' ':!node_modules' ':!.next' ':!.git' ':!*.md.bak' ':!SUMMARY.md' ':!PROJECT_BRIEF.md' ':!course-structure.json' ':!lesson-modules' ':!receipt.jpg' ':!test-rece*'` returns no matches — BRAND-01 satisfied at the source-tree level.
2. Visiting the guest join page in dev shows: hero lockup, description paragraph, four-step ordered list, then the join card with "Join the table" heading + name input + Join button — in that DOM order.
3. `npm run dev` startup log includes the line `> Fare Share ready on http://localhost:<port>`.
4. `npm run build` succeeds — Next.js compiles, lint passes, TypeScript types check.
5. `package.json` name is `fare-share` (kebab-case) and the package is still `"private": true`.
</verification>

<success_criteria>
- BRAND-01 fully satisfied: zero remaining user-visible "Tab Splitter" strings in tracked source (verified by the locked grep above); package metadata, server log, JSDoc tooltips all say "Fare Share". Combined with Plan 09-01's `app/layout.tsx` metadata fix and Plan 09-03's HeaderBar wordmark, BRAND-01 is fully closed across this phase.
- ONBOARD-03 satisfied: locked one-line app description rendered above the join form on the guest page.
- ONBOARD-04 satisfied: locked four-step instruction list rendered as semantic `<ol>` between the description and the join card.
- Plan runs in parallel with 09-03 (Wave 2) — only file overlap is `JoinForm.tsx`, resolved by 09-03's documented insertion-point comment which 09-04 consumes and replaces.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fare-share-rebrand-onboarding/09-04-SUMMARY.md` summarizing:
- JoinForm insertion: confirm description + 4-step ol present, hero + card untouched
- The five string-rename sites: file, line, before/after
- Result of the final-sweep grep (expected: empty)
- Any unexpected "Tab Splitter" occurrences found outside the five known sites and how they were resolved (expected: none)
- Any deviations from PATTERNS.md or UI-SPEC (expected: none)
</output>
